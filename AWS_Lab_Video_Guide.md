# AWS Academy CLO835 - Лабораторная работа №1
## Полная пошаговая инструкция для видео (15 минут)

---

## ПОДГОТОВКА (что нужно перед записью видео)

Перед тем как начать запись видео, убедись что у тебя готово:
- AWS Account с доступом
- GitHub Account 
- Git установлен на компьютере
- Docker установлен на компьютере
- Terraform установлен на компьютере
- SSH ключ для AWS (для подключения к EC2)

---

## СТРУКТУРА ВИДЕО - 4 ОСНОВНЫЕ ЧАСТИ (по 3-4 минуты каждая)

### ✅ ЧТО МЫ ДЕЛАЕМ (объяснение в начале)
**Что ты говоришь в начале видео:**
"В этой лабе мы будем создавать Docker контейнеры с приложением и базой данных, автоматически их собирать через GitHub Actions, и запускать на AWS сервере. Это поможет нам понять как работает контейнеризация и автоматизация развертывания приложений в облаке."

---

# ЧАСТЬ 1: TERRAFORM - РАЗВЕРТЫВАНИЕ ИНФРАСТРУКТУРЫ (4 минуты)

## Шаг 1.1: Создаем папку для Terraform кода

**Команда:**
```bash
mkdir -p terraform-aws-lab
cd terraform-aws-lab
```

**Что происходит:** Создаем отдельную папку для хранения кода, который будет автоматически создавать нужные нам сервисы AWS.

**Что ты показываешь:** Окно терминала, как появляется новая папка.

---

## Шаг 1.2: Создаем основной Terraform файл (main.tf)

**Команда:**
```bash
cat > main.tf << 'EOF'
terraform {
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5.0"
    }
  }
}

provider "aws" {
  region = "us-east-1"
}

# Получаем информацию о VPC по умолчанию
data "aws_vpc" "default" {
  default = true
}

# Получаем public subnet из default VPC
data "aws_subnets" "default_public" {
  filter {
    name   = "vpc-id"
    values = [data.aws_vpc.default.id]
  }
  filter {
    name   = "default-for-az"
    values = [true]
  }
}

# Security Group для EC2
resource "aws_security_group" "ec2_sg" {
  name        = "clo835-ec2-sg"
  description = "Security group for EC2 instance"
  vpc_id      = data.aws_vpc.default.id

  ingress {
    from_port   = 22
    to_port     = 22
    protocol    = "tcp"
    cidr_blocks = ["0.0.0.0/0"]
  }

  ingress {
    from_port   = 8081
    to_port     = 8083
    protocol    = "tcp"
    cidr_blocks = ["0.0.0.0/0"]
  }

  egress {
    from_port   = 0
    to_port     = 0
    protocol    = "-1"
    cidr_blocks = ["0.0.0.0/0"]
  }

  tags = {
    Name = "clo835-ec2-sg"
  }
}

# IAM Role для EC2 (разрешение доступа к ECR)
resource "aws_iam_role" "ec2_role" {
  name = "clo835-ec2-role"

  assume_role_policy = jsonencode({
    Version = "2012-10-17"
    Statement = [
      {
        Action = "sts:AssumeRole"
        Effect = "Allow"
        Principal = {
          Service = "ec2.amazonaws.com"
        }
      }
    ]
  })
}

resource "aws_iam_role_policy" "ecr_policy" {
  name = "ecr-pull-policy"
  role = aws_iam_role.ec2_role.id

  policy = jsonencode({
    Version = "2012-10-17"
    Statement = [
      {
        Effect = "Allow"
        Action = [
          "ecr:GetAuthorizationToken",
          "ecr:BatchGetImage",
          "ecr:GetDownloadUrlForLayer"
        ]
        Resource = "*"
      }
    ]
  })
}

resource "aws_iam_instance_profile" "ec2_profile" {
  name = "clo835-ec2-profile"
  role = aws_iam_role.ec2_role.name
}

# EC2 Instance
resource "aws_instance" "clo835" {
  ami                    = "ami-0c55b159cbfafe1f0"  # Amazon Linux 2
  instance_type          = "t2.micro"
  iam_instance_profile   = aws_iam_instance_profile.ec2_profile.name
  security_groups        = [aws_security_group.ec2_sg.name]
  associate_public_ip_address = true

  user_data = base64encode(<<-EOF
              #!/bin/bash
              yum update -y
              yum install -y docker
              systemctl start docker
              systemctl enable docker
              usermod -a -G docker ec2-user
              yum install -y aws-cli
              EOF
  )

  tags = {
    Name = "clo835-ec2"
  }
}

# ECR Repository для приложения
resource "aws_ecr_repository" "app" {
  name                 = "clo835-app"
  image_tag_mutability = "MUTABLE"

  image_scanning_configuration {
    scan_on_push = false
  }

  tags = {
    Name = "clo835-app"
  }
}

# ECR Repository для MySQL
resource "aws_ecr_repository" "mysql" {
  name                 = "clo835-mysql"
  image_tag_mutability = "MUTABLE"

  image_scanning_configuration {
    scan_on_push = false
  }

  tags = {
    Name = "clo835-mysql"
  }
}

# Outputs
output "ec2_public_ip" {
  value       = aws_instance.clo835.public_ip
  description = "Public IP of EC2 instance"
}

output "ecr_app_uri" {
  value       = aws_ecr_repository.app.repository_url
  description = "ECR URI for app"
}

output "ecr_mysql_uri" {
  value       = aws_ecr_repository.mysql.repository_url
  description = "ECR URI for MySQL"
}
EOF
```

**Что происходит:** Создаем файл с кодом Terraform. Этот код будет:
- Создавать EC2 машину (это наш сервер в облаке)
- Создавать ECR хранилище для Docker образов (место где будут храниться наши контейнеры)
- Настраивать разрешения доступа

**Что ты показываешь:** Экран с содержимым файла (можно открыть в VS Code или текстовом редакторе).

---

## Шаг 1.3: Инициализируем Terraform

**Команда:**
```bash
terraform init
```

**Что происходит:** Terraform скачивает все необходимые плагины и подготавливает рабочую папку. Это надо делать один раз.

**Что ты показываешь:** Вывод команды в терминале, как Terraform говорит "Initialized successfully".

---

## Шаг 1.4: Проверяем что будет создано

**Команда:**
```bash
terraform plan
```

**Что происходит:** Terraform показывает план - что именно будет создано в AWS. Это безопасно, ничего не создается на этом этапе.

**Что ты показываешь:** Вывод команды, где видно что будут созданы: 1 EC2 instance, 2 ECR repositories, Security Group и IAM Role.

---

## Шаг 1.5: Развертываем инфраструктуру!

**Команда:**
```bash
terraform apply -auto-approve
```

**Что происходит:** Terraform создает в облаке:
- Сервер EC2 (это наш компьютер в облаке)
- Два хранилища для Docker образов (ECR для приложения и для MySQL)
- Сетевые правила (какие порты открыты)
- Разрешения доступа

Это займет 2-3 минуты.

**Что ты показываешь:** 
- Вывод терминала как Terraform создает ресурсы
- В конце команды будут выведены IP адреса и URL - скопируй/запиши их! (Они нужны будут позже)

**⚠️ ВАЖНО:** Запиши эти значения:
- `ec2_public_ip` - IP адрес нашего сервера
- `ecr_app_uri` - адрес хранилища для приложения
- `ecr_mysql_uri` - адрес хранилища для MySQL

---

# ЧАСТЬ 2: GITHUB - АВТОМАТИЗАЦИЯ СБОРКИ КОНТЕЙНЕРОВ (4 минуты)

## Шаг 2.1: Создаем GitHub репозиторий для приложения

**Что ты делаешь вручную:**
1. Идешь на GitHub.com
2. Нажимаешь "New Repository" (или "+")
3. Назваешь его: `clo835-app`
4. Оставляешь публичным (Public)
5. Нажимаешь "Create Repository"

**Что происходит:** Создаем место где будет храниться код нашего приложения, и откуда GitHub Actions будет автоматически собирать Docker образы.

**Что ты показываешь:** Экран GitHub с новым репозиторием.

---

## Шаг 2.2: Клонируем репозиторий и копируем файлы приложения

**Команда:**
```bash
cd ~
git clone https://github.com/ladunuthala/clo835_summer2023_assignment1.git
cd clo835_summer2023_assignment1
```

**Что происходит:** Скачиваем готовое приложение с GitHub. Это двухуровневое приложение: есть веб-часть (фронтенд) и база данных (MySQL).

**Что ты показываешь:** Терминал как скачивается репозиторий, и папки с файлами.

---

## Шаг 2.3: Посмотри что там за файлы

**Команда:**
```bash
ls -la
```

**Что происходит:** Показываем что есть в папке:
- `application/` - папка с веб приложением
- `mysql/` - папка с конфигурацией базы данных
- Dockerfile файлы - инструкции как собрать контейнеры

**Что ты показываешь:** Список файлов в терминале.

---

## Шаг 2.4: Создаем свой репозиторий локально

**Команда:**
```bash
cd ~
mkdir clo835-my-app
cd clo835-my-app
git init
git config user.email "your-email@example.com"
git config user.name "Your Name"
```

**Что происходит:** Создаем нашу собственную копию для работы и инициализируем Git.

**Что ты показываешь:** Терминал с командами.

---

## Шаг 2.5: Копируем файлы приложения в наш репозиторий

**Команда:**
```bash
cp -r ~/clo835_summer2023_assignment1/* .
ls -la
```

**Что происходит:** Копируем все файлы (приложение и Dockerfile) в нашу папку.

**Что ты показываешь:** Копирование файлов и список файлов в нашей папке.

---

## Шаг 2.6: Посмотри структуру приложения

**Команда:**
```bash
cat application/Dockerfile
```

**Что происходит:** Показываем Dockerfile - это инструкция как собрать Docker контейнер с приложением. Там написано:
- Используем Python базовый образ
- Устанавливаем зависимости приложения
- Запускаем приложение на порту 8080

**Что ты показываешь:** Содержимое Dockerfile в терминале. Объясни что это инструкция для Docker как собрать контейнер.

---

## Шаг 2.7: Посмотри Dockerfile для MySQL

**Команда:**
```bash
cat mysql/Dockerfile
```

**Что происходит:** Показываем Dockerfile для MySQL - инструкция как собрать контейнер с базой данных.

**Что ты показываешь:** Содержимое MySQL Dockerfile.

---

## Шаг 2.8: Создаем GitHub Actions workflow файл

**Команда:**
```bash
mkdir -p .github/workflows
cat > .github/workflows/build-and-push.yml << 'EOF'
name: Build and Push Docker Images to ECR

on:
  push:
    branches: [ master ]

env:
  REGISTRY: ${{ secrets.AWS_ACCOUNT_ID }}.dkr.ecr.${{ secrets.AWS_REGION }}.amazonaws.com

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          role-to-assume: arn:aws:iam::${{ secrets.AWS_ACCOUNT_ID }}:role/github-actions-role
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Login to Amazon ECR
        uses: aws-actions/amazon-ecr-login@v1
        id: login-ecr

      - name: Build, tag, and push application image to Amazon ECR
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          ECR_REPOSITORY: clo835-app
          IMAGE_TAG: latest
        run: |
          docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG ./application
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG

      - name: Build, tag, and push MySQL image to Amazon ECR
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          ECR_REPOSITORY: clo835-mysql
          IMAGE_TAG: latest
        run: |
          docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG ./mysql
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
EOF
```

**Что происходит:** Создаем GitHub Actions workflow. Это файл с инструкциями для GitHub что делать когда мы загружаем код:
1. Скачивает код
2. Входит в AWS
3. Собирает Docker образ для приложения
4. Отправляет образ в ECR
5. Собирает Docker образ для MySQL
6. Отправляет образ в ECR

**Что ты показываешь:** Содержимое workflow файла (что файл создан и что там внутри).

---

## Шаг 2.9: Защищаем master branch

**Что ты делаешь вручную на GitHub:**
1. Идешь в свой репозиторий `clo835-app`
2. Settings → Branches
3. Нажимаешь "Add rule"
4. Branch name pattern: `master`
5. Включаешь: "Require a pull request before merging"
6. Сохраняешь

**Что происходит:** Теперь нельзя будет напрямую коммитить в master branch. Все изменения должны идти через pull request (просмотр и одобрение). Это нужно для безопасности и чтобы GitHub Actions правильно срабатывал.

**Что ты показываешь:** Экран GitHub Settings с настройками branch protection.

---

## Шаг 2.10: Добавляем AWS credentials в GitHub

**Что ты делаешь вручную на GitHub:**
1. Идешь в Settings → Secrets and variables → Actions
2. Нажимаешь "New repository secret"
3. Создаешь 2 секрета:
   - `AWS_ACCOUNT_ID`: твой AWS account ID (цифры)
   - `AWS_REGION`: `us-east-1`

**Что происходит:** Храним AWS credentials в GitHub безопасно. GitHub Actions будет использовать эти данные чтобы войти в AWS и загрузить образы.

**Что ты показываешь:** Экран GitHub с добавленными secrets.

---

## Шаг 2.11: Первый commit в development branch

**Команда:**
```bash
cd ~/clo835-my-app
git add .
git commit -m "Initial commit with application and MySQL Dockerfile"
git branch development
git checkout development
```

**Что происходит:** Создаем development branch для работы. В production (master) мы будем отправлять только готовый код.

**Что ты показываешь:** Терминал с git командами.

---

## Шаг 2.12: Загружаем код на GitHub

**Команда:**
```bash
git remote add origin https://github.com/YOUR_USERNAME/clo835-app.git
git push -u origin development
```

**ЗАМЕНИ:** `YOUR_USERNAME` на твое имя на GitHub.

**Что происходит:** Загружаем наш код на GitHub в development branch.

**Что ты показываешь:** Терминал с командой push и сообщение об успехе.

---

## Шаг 2.13: Создаем Pull Request и мержим в master

**Что ты делаешь вручную на GitHub:**
1. Идешь в свой репозиторий
2. Видишь сообщение "development had recent pushes"
3. Нажимаешь "Compare & pull request"
4. Нажимаешь "Create pull request"
5. Нажимаешь "Merge pull request"
6. Нажимаешь "Confirm merge"

**Что происходит:** 
- Pull request показывает какие изменения мы хотим внести в master
- После merge в master - GitHub Actions автоматически сработает!
- GitHub Actions скачает код, соберет Docker образы и загрузит их в ECR

**Что ты показываешь:** 
- Экран GitHub с Pull Request
- Экран GitHub Actions (Actions tab) - увидишь workflow как работает
- Подожди пока он завершится (зеленая галочка = успех)
- Проверь в AWS ECR что образы загрузились (AWS Console → ECR)

---

# ЧАСТЬ 3: ЗАПУСК КОНТЕЙНЕРОВ НА EC2 (4 минуты)

## Шаг 3.1: Подключаемся к EC2 через SSH

**Команда:**
```bash
ssh -i /path/to/your/key.pem ec2-user@ТВОЙ_EC2_IP
```

**ЗАМЕНИ:**
- `/path/to/your/key.pem` - путь к твоему SSH ключу
- `ТВОЙ_EC2_IP` - IP адрес который вывел Terraform на шаге 1.5

**Пример:**
```bash
ssh -i ~/Downloads/my-key.pem ec2-user@54.123.45.67
```

**Что происходит:** Подключаемся к нашему серверу в облаке AWS.

**Что ты показываешь:** Терминал как ты подключился и видны команды AWS.

---

## Шаг 3.2: Проверяем что Docker работает

**Команда:**
```bash
docker --version
docker ps
```

**Что происходит:** Проверяем что Docker установлен и готов работать. `docker ps` показывает запущенные контейнеры (сейчас пусто).

**Что ты показываешь:** Вывод команды в терминале.

---

## Шаг 3.3: Логинимся в AWS ECR

**Команда:**
```bash
aws ecr get-login-password --region us-east-1 | docker login --username AWS --password-stdin ТВОЙ_ECR_URL
```

**ЗАМЕНИ:** `ТВОЙ_ECR_URL` на адрес ECR от Terraform (например `123456789.dkr.ecr.us-east-1.amazonaws.com`)

**Что происходит:** Входим в AWS хранилище образов. После этого Docker может скачивать наши образы.

**Что ты показываешь:** Терминал, сообщение "Login Succeeded".

---

## Шаг 3.4: Создаем custom network для контейнеров

**Команда:**
```bash
docker network create clo835-network
```

**Что происходит:** Создаем специальную сеть для наших контейнеров. Это нужно чтобы контейнеры могли общаться друг с другом по имени (например пинговать друг друга).

**Что ты показываешь:** Терминал с выводом ID network.

---

## Шаг 3.5: Скачиваем MySQL образ из ECR

**Команда:**
```bash
docker pull ТВОЙ_ECR_URL/clo835-mysql:latest
```

**ЗАМЕНИ:** `ТВОЙ_ECR_URL` на URL из Terraform

**Что происходит:** Скачиваем Docker образ MySQL из нашего хранилища ECR.

**Что ты показываешь:** Терминал как скачивается образ (это займет 30-60 секунд).

---

## Шаг 3.6: Скачиваем Application образ из ECR

**Команда:**
```bash
docker pull ТВОЙ_ECR_URL/clo835-app:latest
```

**Что происходит:** Скачиваем Docker образ приложения из ECR.

**Что ты показываешь:** Терминал как скачивается образ.

---

## Шаг 3.7: Проверяем что образы скачались

**Команда:**
```bash
docker images
```

**Что происходит:** Показываем все скачанные Docker образы. Должны быть видны: `clo835-mysql` и `clo835-app`.

**Что ты показываешь:** Список образов в терминале.

---

## Шаг 3.8: Запускаем MySQL контейнер

**Команда:**
```bash
docker run -d \
  --name mysql-container \
  --network clo835-network \
  -e MYSQL_ROOT_PASSWORD=root123 \
  -e MYSQL_DATABASE=appdb \
  ТВОЙ_ECR_URL/clo835-mysql:latest
```

**ЗАМЕНИ:** `ТВОЙ_ECR_URL` на адрес из Terraform

**Что происходит:** Запускаем контейнер с базой данных MySQL:
- `-d` = запускаем в фоне (не блокирует терминал)
- `--name mysql-container` = имя контейнера (им он будет называться)
- `--network` = подключаем к нашей сети (чтобы приложение могло подключиться)
- `-e MYSQL_ROOT_PASSWORD` = задаем пароль admin
- `-e MYSQL_DATABASE` = создаем базу данных с именем `appdb`

**Что ты показываешь:** Терминал, ID запущенного контейнера.

---

## Шаг 3.9: Проверяем что MySQL работает

**Команда:**
```bash
docker ps
docker logs mysql-container
```

**Что происходит:** 
- `docker ps` = показывает запущенные контейнеры
- `docker logs` = показывает логи (вывод) контейнера

Должно быть видно что MySQL запустился успешно.

**Что ты показываешь:** Терминал с запущенным контейнером и логами.

---

## Шаг 3.10: Запускаем APPLICATION контейнер BLUE (порт 8081)

**Команда:**
```bash
docker run -d \
  --name blue \
  --network clo835-network \
  -p 8081:8080 \
  -e BACKGROUND_COLOR=blue \
  -e DB_HOST=mysql-container \
  -e DB_USER=root \
  -e DB_PASSWORD=root123 \
  -e DB_NAME=appdb \
  ТВОЙ_ECR_URL/clo835-app:latest
```

**ЗАМЕНИ:** `ТВОЙ_ECR_URL` на адрес из Terraform

**Что происходит:** Запускаем приложение с синим фоном:
- `--name blue` = имя контейнера
- `-p 8081:8080` = перенаправляем порт: внешний 8081 → внутри контейнера 8080
- `-e BACKGROUND_COLOR=blue` = приложение будет иметь синий фон
- `-e DB_HOST=mysql-container` = адрес базы данных (имя контейнера MySQL)
- Остальные `-e` = параметры для подключения к MySQL

**Что ты показываешь:** Терминал, ID контейнера.

---

## Шаг 3.11: Запускаем APPLICATION контейнер PINK (порт 8082)

**Команда:**
```bash
docker run -d \
  --name pink \
  --network clo835-network \
  -p 8082:8080 \
  -e BACKGROUND_COLOR=pink \
  -e DB_HOST=mysql-container \
  -e DB_USER=root \
  -e DB_PASSWORD=root123 \
  -e DB_NAME=appdb \
  ТВОЙ_ECR_URL/clo835-app:latest
```

**Что происходит:** То же самое что blue, но с розовым фоном и портом 8082.

**Что ты показываешь:** Терминал, ID контейнера.

---

## Шаг 3.12: Запускаем APPLICATION контейнер LIME (порт 8083)

**Команда:**
```bash
docker run -d \
  --name lime \
  --network clo835-network \
  -p 8083:8080 \
  -e BACKGROUND_COLOR=lime \
  -e DB_HOST=mysql-container \
  -e DB_USER=root \
  -e DB_PASSWORD=root123 \
  -e DB_NAME=appdb \
  ТВОЙ_ECR_URL/clo835-app:latest
```

**Что происходит:** То же самое что blue и pink, но с лайм фоном и портом 8083.

**Что ты показываешь:** Терминал, ID контейнера.

---

## Шаг 3.13: Проверяем что все 4 контейнера работают

**Команда:**
```bash
docker ps
```

**Что происходит:** Показываем все 4 работающих контейнера (mysql-container, blue, pink, lime).

**Что ты показываешь:** Терминал, список всех контейнеров.

---

## Шаг 3.14: Проверяем логи контейнеров

**Команда:**
```bash
docker logs blue
docker logs pink
docker logs lime
```

**Что происходит:** Смотрим логи каждого контейнера - должны быть строки что приложение запустилось успешно.

**Что ты показываешь:** Терминал с логами.

---

## Шаг 3.15: Открываем приложение в браузере

**Действие:** Открой браузер и напиши:
```
http://ТВОЙ_EC2_IP:8081
http://ТВОЙ_EC2_IP:8082
http://ТВОЙ_EC2_IP:8083
```

**ЗАМЕНИ:** `ТВОЙ_EC2_IP` на IP адрес твоей EC2 (из Terraform output)

**Что происходит:** Видим что все три приложения работают и доступны из интернета (с разными цветами фонов).

**Что ты показываешь:** Браузер, три приложения с разными цветами.

---

## Шаг 3.16: Проверяем что контейнеры видят друг друга (ping по имени)

**Команда:**
```bash
docker exec blue ping -c 3 pink
docker exec blue ping -c 3 lime
docker exec pink ping -c 3 blue
docker exec pink ping -c 3 lime
docker exec lime ping -c 3 blue
docker exec lime ping -c 3 pink
```

**Что происходит:** Проверяем что контейнеры могут общаться друг с другом через custom network:
- `docker exec` = запустить команду внутри контейнера
- `ping pink` = отправить сигнал контейнеру pink (по имени, не по IP!)
- `-c 3` = отправить 3 пакета

Все должны пройти успешно (0% packet loss).

**Что ты показываешь:** Терминал, результаты ping команд. ВАЖНО: покажи что ping работает ПО ИМЕНИ (ping pink, не по IP).

---

## Шаг 3.17: Объяснение почему 3 приложения могут слушать один порт

**Что ты говоришь в видео:**
"Все три приложения внутри контейнеров слушают на одном порту 8080. Но они могут это делать потому что находятся в разных контейнерах, у каждого свое изолированное окружение. 

На EC2 машине мы открыли разные порты (8081, 8082, 8083) которые направляют трафик в контейнеры. Это называется port mapping:
- 8081 → blue контейнер порт 8080
- 8082 → pink контейнер порт 8080  
- 8083 → lime контейнер порт 8080

Контейнеризация изолирует приложения друг от друга - это основная фишка Docker. Каждый контейнер думает что он на своей машине и может использовать любой порт."

**Что ты показываешь:** Экран с приложениями или диаграмма (если можешь нарисовать на доске/бумаге).

---

# ЧАСТЬ 4: ФИНАЛЬНЫЕ ДЕМОНСТРАЦИИ (3-4 минуты)

## Финальная демонстрация 1: GitHub Actions успешно собрал образы

**Что ты показываешь:**
1. GitHub → Actions tab
2. Видно что workflow успешно выполнился (зеленая галочка)
3. GitHub → Code tab
4. AWS Console → ECR
5. Видно что образы там загружены (clo835-app и clo835-mysql)

**Что ты говоришь:**
"Смотри, когда мы залили код в master branch через pull request, GitHub Actions автоматически:
1. Скачал код
2. Собрал Docker образ приложения (из Dockerfile)
3. Собрал Docker образ MySQL
4. Загрузил оба образа в Amazon ECR

Это полная автоматизация. В будущем любые изменения кода автоматически пересоберут и обновят образы."

---

## Финальная демонстрация 2: Все контейнеры работают

**Команда:**
```bash
docker ps
curl localhost:8081
curl localhost:8082
curl localhost:8083
```

**Что ты показываешь:**
- Список всех 4 контейнеров (mysql + blue + pink + lime)
- Результат curl команд (HTML кода приложения)

**Что ты говоришь:**
"На EC2 машине у нас работают все контейнеры:
1. MySQL база данных
2. Три копии приложения на разных портах

Все изолированы друг от друга, но могут общаться через custom bridge network."

---

## Финальная демонстрация 3: Приложения доступны из интернета

**Что ты показываешь:**
- Три открытых вкладки браузера с приложениями на портах 8081, 8082, 8083
- Каждое имеет разный цвет фона

**Что ты говоришь:**
"Приложения доступны с интернета через публичный IP адрес EC2. Это возможно потому что:
1. EC2 находится в public subnet
2. Security Group открывает порты 8081-8083
3. Трафик может доходить от интернета до контейнеров"

---

## Финальная демонстрация 4: Ping между контейнерами

**Команда:**
```bash
docker exec blue ping -c 2 pink
docker exec blue ping -c 2 lime
docker exec pink ping -c 2 blue
```

**Что ты показываешь:** Терминал, успешные ping ответы.

**Что ты говоришь:**
"Контейнеры могут общаться друг с другом через custom bridge network. Они видят друг друга по имени (pink, lime, blue) - это DNS разрешение внутри Docker сети. 

Это важно потому что приложение может подключиться к MySQL контейнеру просто написав DB_HOST=mysql-container, вместо использования IP адреса."

---

# КОНЕЦ ВИДЕО - ИТОГИ

**Что ты говоришь в конце:**

"Мы успешно выполнили лабораторную работу! Вот что мы сделали:

1. **Terraform** - развернули инфраструктуру в AWS (EC2 + ECR)
2. **GitHub Actions** - настроили автоматическую сборку Docker образов при изменении кода
3. **Docker на EC2** - запустили контейнеры с приложением и MySQL
4. **Сетевая часть** - контейнеры видят друг друга по имени через custom network

Это цикл CI/CD (Continuous Integration / Continuous Deployment):
- Мы изменяем код → GitHub Actions автоматически собирает образ → образ загружается в ECR → EC2 может быстро запустить новую версию

Это основа современной разработки и DevOps!"

---

# ДОПОЛНИТЕЛЬНАЯ ИНФОРМАЦИЯ ДЛЯ ПОНИМАНИЯ

## Что такое Docker?
Docker - это контейнеризация. Представь что приложение это не просто файлы, а целая коробка с приложением + все что ему нужно (библиотеки, конфиги, ОС). Когда запускаешь эту коробку (контейнер) - всё работает одинаково везде.

## Что такое Terraform?
Terraform - это Infrastructure as Code. Вместо того чтобы кликать мышкой в AWS консоли и создавать ресурсы вручную, пишешь код что нужно создать. Terraform читает код и создает ресурсы автоматически.

## Что такое GitHub Actions?
GitHub Actions - это роботизация. Когда ты загружаешь код на GitHub, можно настроить автоматический workflow. Например: собери Docker образ → отправь в ECR. Это экономит время и убирает ошибки.

## Что такое ECR?
ECR (Elastic Container Registry) - это хранилище Docker образов в облаке AWS. Это как Docker Hub но в приватном облаке AWS.

## Что такое IAM?
IAM (Identity and Access Management) - это система разрешений. Мы даем EC2 разрешение скачивать образы из ECR (pull access). Без этого EC2 не может скачать образы.

## Что такое Security Group?
Security Group - это firewall. Мы говорим какие порты открыты для входящего трафика (22 для SSH, 8081-8083 для приложений).

## Custom Bridge Network в Docker?
Это специальная сеть где контейнеры видят друг друга по имени. Если у тебя есть контейнер с именем "pink", другой контейнер может обратиться к нему просто написав "pink" (Docker сам разрешит имя в IP адрес).

---

## ПРОВЕРОЧНЫЙ СПИСОК ДЛЯ ВИДЕО

Убедись что в видео видно все эти пункты:

✅ Terraform применяется и создает ресурсы
✅ Вывод IP адреса EC2 и URL ECR
✅ Подключение к EC2 через SSH
✅ Docker login в ECR
✅ Скачивание образов из ECR (docker pull)
✅ Создание custom network
✅ Запуск MySQL контейнера
✅ Запуск 3 контейнеров приложения (blue, pink, lime) на портах 8081, 8082, 8083
✅ docker ps показывает все 4 контейнера
✅ Браузер открывает все три приложения на разных портах
✅ Ping между контейнерами работает (по имени)
✅ GitHub Actions workflow показывает успешно выполненное собрание образов
✅ ECR показывает загруженные образы

## ПРИМЕРНОЕ ВРЕМЯ НА КАЖДЫЙ ЭТАП

- Подготовка GitHub: 2 мин
- Terraform deploy: 4 мин (+ время на ожидание AWS)
- GitHub Actions build: 3 мин (+ время на выполнение workflow)
- Запуск контейнеров: 4 мин
- Демонстрация приложений: 2 мин

**ИТОГО: примерно 15 минут**

---

Удачи! Следуй этой инструкции пошагово и всё получится! 🚀
